<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.browser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".url.URL_DownloadActivity" />
        <!--<activity android:name=".chat.messenger.MessengerActivity" />-->
        <!--<activity-->
            <!--android:name=".chat.register.LoginActivity"-->
            <!--android:label="@string/title_activity_login" />-->
        <!--<activity android:name=".chat.user_list.UsersListActivity"></activity>-->
    </application>

</manifest>